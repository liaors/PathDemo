<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rs.pathdemo">

    <application xmlns:tool="http://schemas.android.com/tools"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tool:ignore="LockedOrientationActivity">

        <activity
            android:name="com.rs.pathdemo.activity.GLActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.rs.pathdemo.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.rs.pathdemo.activity.PathViewActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>